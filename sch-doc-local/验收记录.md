# 交付验收记录

日期：2026-09-15初建，2026-09-16继续验收。工作目录：`E:\Codex\Self-Dev\Sch-Doc\研招监控助手`。

## 已交付

- 中文本地浏览器程序、双击启动入口、可选Windows每日检查设置与取消脚本。
- 18个预设来源：17个启用、江西省教育厅入口默认暂停。含列表、单页和PDF监控。
- 29条首批研究笔记，含已核对事实、推算、缺证据项和独立标记的第三方线索；另通过实际窗口保存1条用户2028入学准备清单，9月16日补记1条已失效的南昌大学官方公示附件线索，共31条。
- 12个已定位的官方资料网址已加入归档队列；入队和保存成功分别记录，不将待保存任务算成原件。
- 按学校、年份分类和搜索；笔记新增与编辑；未读变更；HTML/文本/附件版本归档；手动文件导入；JSON/CSV导出。
- `README.md`、首批研究与准备路线、按来源核验清单、真实首轮错误记录和便携ZIP。

## 已通过的程序验证

1. **16项核心测试通过**：首次基线/新增通知、重复不重存、列表变更、失败保留基线、旧条目保留、正文与PDF修订、错误PDF拒收、认证跳转、附件失败状态、中文编码、日期来源、分页/嵌入附件、跨进程互斥、数据导出等。
2. **12项完整HTTP链路检查通过**：独立临时数据库 + 本地合成站点，通过真实HTTP完成列表→正文→PDF→归档→下载；新增提醒；笔记新增/修改/关联；手动导入；导出；重启持久化；暂停保留历史；跨站写入防护。
3. **浏览器实测**：真实本地窗口可打开；学校+年份筛选可定位华工2026资料；实际表单保存2028个人准备清单，刷新后仍能搜索到；桌面及390px窄窗口截图已留存。
4. Python功能测试、JavaScript语法检查、PowerShell启动脚本语法检查均通过。
5. 9月16日通过交付的 `boot.ps1 -Mode start` 启动成功；实际Chrome打开中文窗口，30条原有笔记保留。命令行采集时，浏览器能显示“检查进行中”并禁用重复检查按钮。新增失效链接线索后共31条。
6. **便携包7项检查通过**：ZIP校验、SQLite备份完整性、解压到不同目录后真实启动、中文页面、2028目标及31条笔记保留、解压副本导出、正常退出。打包不包含浏览器开发测试依赖。最终ZIP的SHA-256和验收结果记录在项目目录的 `research/便携包验收.json`，该报告置于ZIP之外以便校验包本身。

测试数据只放在临时目录，测试结束移除；没有写入用户资料库，没有把合成网页呈现成真实招生信息。

## 尚未通过的真实采集验收（必须保留此边界）

**首轮17个启用来源均出现HTTPS连接失败；截至本次交付，自动发现条目0，官网原始归档0。**

9月16日最终重试：17个启用来源仍失败，12个已加入的官方资料归档链接也均失败，最终原件保存数仍为0。完整数据导出保留31条研究/个人笔记及真实错误状态。

- Python直接请求、显式采用已有系统代理、curl、独立Chrome的学校网址访问均失败；普通Python官网作为连通性对照可返回200。
- 9月16日重试南昌大学、华工、重大、过程所、大连化物所仍出现TLS中断；南昌大学使用公开DNS返回的真实IPv4做进程内连接测试亦失败，不能简单认定仅是DNS问题。未修改系统网络设置。
- 本次联网检索工具可读取部分官方页面/文件，所以已完成带网址的研究笔记；这不等于本机下载了那些原件。
- 失败状态已真实写入数据库及 `data/last_check.log`，未被写成“无更新”。
- 各站专用解析函数依据已观察到的公开链接形态编写，**没有宣称已经在这些学校的原始HTML上通过实测**。恢复网络后仍要在来源页逐项验收；改版或非静态页面可能需要维护。
- 南昌大学历年教育部下达推荐名额、学院/专业推荐分配未取得足够官方证据；保留空值及询问模板。全国高校完整录取数据也未获取，不声称全量覆盖。

网络恢复后的操作：双击启动 → 立即检查全部 → 检查每源状态 → 继续归档队列 → 核对“资料归档”里实际文件。紧急资料可以用正常浏览器下载后手动导入。

## 定时任务状态

本次**未自动安装或启用Windows每日计划任务**。已经交付可双击的设置脚本，默认09:00；需要Windows已登录并且有网络。关闭电脑不能抓取，错过时间只在后续满足条件时补跑。软件没有创建Codex提醒，没有给他人发送消息。

## 证据文件

- `research/HTTP链路验收.json`：12项检查的结果和隔离边界。
- `research/启动验收.json`：交付启动器、当前数据及计划任务状态。
- `research/实际交付状态.json`：从当前数据库导出的真实来源状态与计数。
- `research/来源核验清单.md`：每个入口的状态及解析适配限制。
- `research/界面_概览.png`、`research/界面_窄窗口.png`：实际窗口截图。
- `research/*.json`：早期网络探测的请求时间、响应或错误；不是研招历史归档。

如需下一次继续：先解决学校官网本机HTTPS连通性，再以真实页面逐源适配，保存关键PDF并按专业、统考/推免、专项等口径统计。不要先把待核实人数填成确定值。
